language: swift
osx_image: xcode10
before_install:
  - cd Example
  - bundle install
  - pod install --repo-update
  - echo -e "machine github.com\n  login $CI_USER_TOKEN" >> ~/.netrc
script:
  - fastlane scan
env:
  global:
cache:
  - report.html